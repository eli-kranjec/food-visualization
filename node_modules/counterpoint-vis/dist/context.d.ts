import { Advanceable } from './ticker';
export declare enum ContrastPreference {
    none = "no-preference",
    more = "more",
    less = "less"
}
export declare enum ColorSchemePreference {
    light = "light",
    dark = "dark"
}
/**
 * A class that contains information about the context in which marks are
 * rendered, as well as user-defined properties of the context.
 */
export declare class RenderContext implements Advanceable {
    private _prefersReducedMotion;
    private _prefersReducedTransparency;
    private _contrastPreference;
    private _colorSchemePreference;
    private _hasChanged;
    constructor();
    _handleMotion(e: MediaQueryListEvent): void;
    _handleTransparency(e: MediaQueryListEvent): void;
    _handleContrastMore(e: MediaQueryListEvent): void;
    _handleContrastLess(e: MediaQueryListEvent): void;
    _handleContrastNone(e: MediaQueryListEvent): void;
    _handleColorSchemeLight(e: MediaQueryListEvent): void;
    _handleColorSchemeDark(e: MediaQueryListEvent): void;
    get prefersReducedMotion(): boolean;
    get prefersReducedTransparency(): boolean;
    get contrastPreference(): ContrastPreference;
    get colorSchemePreference(): ColorSchemePreference;
    advance(dt: number): boolean;
}
export declare function getRenderContext(): RenderContext;
